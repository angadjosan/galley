import { type JSX } from 'react';
import { type DesignBlock } from '@galley/design';
export declare function Palette({ mode, disabled, onAdd, }: {
    /** Light or dark, so a preview matches the canvas it will land on. */
    mode: string;
    disabled: boolean;
    onAdd(block: DesignBlock): void;
}): JSX.Element;
//# sourceMappingURL=Palette.d.ts.map